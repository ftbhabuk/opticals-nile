# Nile Opticals

Landing page for Nile Opticals eyewear shop, Newroad, Pokhara.

Built with Vite + React 19 + Tailwind CSS v4.

```bash
npm install
npm run dev      # dev server at localhost:5173
npm run build    # outputs to dist/
```